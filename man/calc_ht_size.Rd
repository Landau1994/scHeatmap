% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ht_size.R
\name{calc_ht_size}
\alias{calc_ht_size}
\title{Calculate the rendered size of a ComplexHeatmap}
\usage{
calc_ht_size(ht, unit = "inch")
}
\arguments{
\item{ht}{A \code{Heatmap} or \code{HeatmapList} object.}

\item{unit}{Output unit understood by grid, such as \code{"inch"}, \code{"mm"}, or
\code{"cm"}.}
}
\value{
A named numeric vector with elements \code{width} and \code{height}.
}
\description{
\code{calc_ht_size()} is a convenience wrapper around
\code{\link[ComplexHeatmap:ht_size]{ComplexHeatmap::ht_size()}}. It converts the returned grid units into a
numeric width and height suitable for graphics devices such as
\code{\link[grDevices:pdf]{grDevices::pdf()}} or \code{\link[grDevices:png]{grDevices::png()}}.
}
\details{
For reproducible absolute dimensions, set \code{width} and \code{height} (or
\code{heatmap_width} and \code{heatmap_height}) when constructing the heatmap. A
heatmap with fully flexible dimensions may otherwise expand to the active
graphics device.
}
\examples{
mat <- matrix(rnorm(25), nrow = 5)
ht <- ComplexHeatmap::Heatmap(
  mat,
  width = grid::unit(30, "mm"),
  height = grid::unit(30, "mm")
)
calc_ht_size(ht, unit = "inch")
}
